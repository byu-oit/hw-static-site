version: 2

updates:
  # This is redundant in this template, but wil be helpful one a real JavaScript site is is built in the "src" folder
  - package-ecosystem: "npm"
    directory: /src
    schedule:
      interval: daily
    target-branch: dev # Our dev pipeline runs off the dev branch, production runs off of master
#    reviewers:
#      - YourGitHubUsername